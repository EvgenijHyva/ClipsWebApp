// https://jasmine.github.io/api/4.3/matchers

describe('App component', ()=> {
	it('Sanity test: should pass (true = true), verify the tool (jasmin) is working', () => {
		expect(true).toBeTruthy();
	});
} );

